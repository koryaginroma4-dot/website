<div class="categories-container" id="products">
    <section class="categories">
        {% for category in categories %}
            {% include 'Home/Widget/categories/category.html.twig' with { 'category': category } %}
        {% endfor %}
    </section>
</div>